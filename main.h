#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdarg.h>
#include <unistd.h>
#include <stddef.h>


    /* fuction prototypes */
    int _printf(const char *format, ...);
    int _putchar(char c);
    int _strlen(const char *s);
    int _puts(const char *str);
    int print_dec(int d);

#endif
